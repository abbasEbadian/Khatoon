/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['statics.basalam.com', '192.168.43.190', "images.unsplash.com", 'khatooneziba.ir'],
  },
}

module.exports = nextConfig
